// function filterUsers(event, usersArray) {
//     const inputValue = event.target.value.toLowerCase();
//     const filteredArray = usersArray.filter((usersArray) => {
//         if (usersArray.name.toLowerCase().startsWith(inputValue)) {
//             console.log(filteredArray);
//             return;
//         }
//     });

//     // - Empty the container that contains the unfiltered products array
//     // - Loop resultArray and build new product for each object in resultArray
// };

// searchUsersInput.addEventListener('keyup', filterUsers);